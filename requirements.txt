# Runtime deps, pinned. Transitive deps (onnxruntime, tokenizers,
# huggingface-hub, pydantic, starlette, ...) are resolved by these.
# Test-only deps live in requirements-dev.txt so they stay out of the image.
fastembed==0.8.0
fastapi==0.139.0
uvicorn==0.49.0
# uvicorn[standard] minus the extras this service never uses: websockets (the API is
# HTTP-only), watchfiles (--reload is dev-only), python-dotenv (no --env-file). uvicorn's
# loop="auto"/http="auto" pick these two up when installed. (PyYAML still arrives
# transitively via huggingface_hub — dropping the extra doesn't remove it.)
uvloop==0.22.1
httptools==0.8.0
numpy==2.5.0
